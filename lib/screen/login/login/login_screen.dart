import 'package:flutter/material.dart';
import 'package:flutterme/screen/login/login/component/body.dart';

class LoginScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(
      ),
    );
  }
}
